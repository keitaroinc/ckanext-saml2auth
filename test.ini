[DEFAULT]
debug = false
smtp_server = localhost
error_email_from = paste@localhost

[server:main]
use = egg:Paste#http
host = 0.0.0.0
port = 5000

[app:main]
use = config:../ckan/test-core.ini

# Insert any custom config settings to be used when running your extension's
# tests here.

## ckanext-saml2auth settings

ckanext.saml2auth.idp_metadata.location = local
ckanext.saml2auth.idp_metadata.local_path = /home/dule/keitaro/ckan29/src/ckanext-saml2auth/ckanext/saml2auth/idp.xml
ckanext.saml2auth.user_firstname = name
ckanext.saml2auth.user_lastname = lastname
ckanext.saml2auth.user_email = email
ckanext.saml2auth.enable_ckan_internal_login = true
ckanext.saml2auth.sysadmins_list = dule@keitaro.com dule.bogdanovski@gmail.com dusko.bogdanovski@keitaro.com


# Logging configuration
[loggers]
keys = root, ckan, sqlalchemy

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console

[logger_ckan]
qualname = ckan
handlers =
level = INFO

[logger_sqlalchemy]
handlers =
qualname = sqlalchemy.engine
level = WARN

[handler_console]
class = StreamHandler
args = (sys.stdout,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(asctime)s %(levelname)-5.5s [%(name)s] %(message)s